# 写算法题是为了学会思维，所以就算python自带模块，也应该自己手写吗？
# 联系自定义模块和底层思维

# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def mergeKLists(self, lists):
        """
        :type lists: List[ListNode]
        :rtype: ListNode
        """